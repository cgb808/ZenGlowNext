# Environment overrides for spool watcher + notifier
# Copy to /etc/default/spool-watcher and adjust paths for your host

# Spool thresholds and script
SPOOL_DIR=/data/spool
SPOOL_MIN_BYTES=4000000
SPOOL_MIN_FILES=1
SPOOL_POLL_INTERVAL=5
SPOOL_PROCESS_SCRIPT=/home/cgbowen/AIWorkspace/scripts/process_spool.sh

# Notifier (Supabase Edge Function)
GATE_URL=https://<project>.functions.supabase.co/redis-pubsub
GATE_TOKEN=your-anon-or-service-key
