# Sanitized snapshot of .env for documentation and onboarding.
# Do NOT use these values in production.

# Core service connectivity
API_EXTERNAL_URL=http://localhost:8000  # change in production
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long

# Postgres (dev defaults)
POSTGRES_HOST=db
POSTGRES_DB=rag_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=__REDACTED__
DATABASE_URL=postgresql://postgres:__REDACTED__@db:5432/rag_db
PII_DATABASE_URL=postgresql://postgres:__REDACTED__@db_pii:5432/rag_pii

# Supabase (optional)
# SUPABASE_DB_URL=postgresql://postgres:__REDACTED__@db.<project>.supabase.co:6543/postgres

# Redis
REDIS_HOST=host.docker.internal
REDIS_PORT=6379

# Ollama / Models
OLLAMA_HOST=host.docker.internal
OLLAMA_PORT=11434
OLLAMA_MODEL=gemma:2b
LEONARDO_URL=http://leonardo:11434
LEONARDO_MODEL=mistral:7b
WHISPER_SERVER_URL=http://whisper:9000

# Embedding / RAG
PG_EMBED_DIM=768
RAG_TOP_K_DEFAULT=5

# Notes:
# - Keep the real secrets only in your local .env (which is .gitignored).
# - For production, rotate JWT_SECRET and database credentials, and update API_EXTERNAL_URL.
