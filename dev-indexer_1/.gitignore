# Root ignore (created)
__pycache__/
*.pyc
.venv/
.env
# Explicitly allow committing sanitized env snapshot
!.env.redacted
.mypy_cache/
.ruff_cache/
.pytest_cache/
node_modules/
*.log
*.tmp
*.swp
*.swo
*.tar.gz
*.zip
models/weights/
artifact/
Artifact/
# Whisper / audio models (excluded)
vendor/whisper.cpp/models/
vendor/whisper.cpp/ggml-*.bin
tests/whisper/
.wdmc/

# Local large artifacts and transients (keep locally, do not track)
# Model bundles and checkpoints
app/inference/model_bundle/

# Whisper.cpp build and model cache
whisper.cpp/

# Local tarballs or archives
.models.tar

# Docker/DB volumes (permissions + huge)
volumes/

# Supabase CLI temp files
supabase/.temp/
