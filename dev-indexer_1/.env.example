## Opt-in GPU probing (disabled by default to avoid CUDA/NVML noise on non-NVIDIA hosts)
# ENABLE_GPU_PROBE=1

## Postgres DSN for family context persistence (leave unset for in-memory only)
# FAMILY_PG_DSN=postgresql://user:pass@localhost:5432/mydb

## Comma-separated admin user IDs (for docs / future dynamic wiring)
# FAMILY_ADMIN_USERS=charles,nancy

