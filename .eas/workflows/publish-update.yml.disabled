name: Publish update

on:
  push:
    branches: ['main', 'develop', 'staging']

jobs:
  update:
    name: Update
    type: update
    params:
      branch: ${{ github.ref_name || 'develop' }}
      message: 'Update from ${{ github.ref_name }} branch - ${{ github.event.head_commit.message }}'
